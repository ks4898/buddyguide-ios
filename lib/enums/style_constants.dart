import 'package:flutter/material.dart';

const String weatherApiKey = "8c618966b3e8e4579ebcf2e8d4066667";

// Containers' width.
const double divWidth = 0.825;

// Main colors.
const white = Colors.white;
const shadowBlack = Color.fromRGBO(0, 0, 0, .25);
const darkBlue = Color.fromRGBO(61, 71, 107, 1);

// Low opacity colors.
const oDarkBlue = Color.fromRGBO(75, 86, 125, .6);

// Gradient colors.
const gDarkBlue = Color.fromRGBO(75, 86, 125, 1);
const gLightBlue = Color.fromRGBO(97, 120, 153, 1);

// Navigation icons.
const List<String> imgPaths = [
  "assets/media/home.png",
  "assets/media/location.png",
  "assets/media/feed.png",
  "assets/media/waste.png",
];

// Radio button options.
final List<String> radioOptions = ["Minor", "Decent", "Major"];
